# Netlify + Next.js 16 uses the automatic OpenNext adapter.
# It manages the publish directory itself, so `publish` is intentionally omitted.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"
